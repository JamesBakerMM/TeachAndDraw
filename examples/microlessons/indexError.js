import { tad, text, make } from "../../lib/TeachAndDraw.js";
tad.use(update);

const example = make.group();
example.push("a");
example.push("b");
example.push("c");
example.push("d");
example.push("e");

function update() {
    exam.push();
}
